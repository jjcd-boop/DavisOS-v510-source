# Forge package contract
A permanent Forge package will contain: manifest, FDL3 source, validated FBC3 image, resources, integrity digest, requested broker capabilities, generation provenance and test report.
Installation is intentionally not implemented by ForgeRunner. A future package manager must revalidate the package and choose the security domain/capabilities; executable metadata may never grant itself authority.
